# GitIgnore for CodeIgniter 4 (CI4) Projects

# Environment Files
# .env files should never be under version control for security reasons.
.env
.env.development
.env.production
.env.testing

# Composer specific
vendor/

# Writable directories - CI4's temporary files, caches, logs, etc.
# Keep the index.html files to ensure directory structure is maintained in Git
writable/cache/*
!writable/cache/index.html
writable/logs/*
!writable/logs/index.html
writable/session/*
!writable/session/index.html
writable/uploads/*
!writable/uploads/index.html
writable/debugbar/*
!writable/debugbar/.gitkeep

# Build artifacts
builds/

# PHPUnit test coverage reports and configuration
tests/coverage/
phpunit.xml # If you have a local phpunit.xml that's different from phpunit.xml.dist
# phpunit.xml.dist # Usually kept under version control, but can be ignored if desired.

# OS specific temporary files
.DS_Store        # macOS
Thumbs.db        # Windows
ehthumbs.db      # Windows
Desktop.ini      # Windows
*~               # Linux/Unix temp files
~$*              # Windows temporary files

# IDE specific files
.idea/             # JetBrains IDEs (PhpStorm, IntelliJ, etc.)
*.iml              # JetBrains project files
.vscode/           # Visual Studio Code
*.sublime-workspace # Sublime Text workspace
*.sublime-project  # Sublime Text project
.phpintel          # PHP Intel (Sublime Text plugin)
/nbproject/        # NetBeans
/build/            # NetBeans
/dist/             # NetBeans

# Other common temporary or build files
php_errors.log     # PHP error log file
/results/          # Example: custom test results directory

.cursor
.gitignore
build/